# Testing

## Static Validation
- [x] Run `luac5.1 -p` on all `.lua` files in `EllesmereUI_Compat`.

## In-Client Tests
- [ ] Load the addon on a 3.3.5 client.
- [ ] Verify there are no fatal Lua errors on startup.
- [ ] Verify the compatibility namespace (`EUICompat`) is accessible.
